#include <cmath>
#include <iostream>
#include <string>
using namespace std;
const int N=5;

int NWD(int a, int b){
    int pom;
    if(a==0 || b==0) return false;
    while(b>0){
        pom=b;
        b=a%b;
        a=pom;
    }
    return a;
}

int delFirst(int n){
    int c=0;
    int p=1;
    while(n>9){
        c+=n%10*p;
        p*=10;
        n/=10;
    }
    return c;
}

int getFirst(int n){
    while(n>9)
        n/=10;
    return n;
}

void sum(int n, int a, int b,int &s){
    if(n==0){
        if(NWD(a,b)==1){
            cout<<a<<" "<<b<<endl;
            s++;
        }
        return;
    }
    
    sum(delFirst(n),a*10+getFirst(n),b,s);
    sum(delFirst(n),a,b*10+getFirst(n),s);
}

int supSum(int N){
    int s=0;
    sum(N,0,0,s);
    return s;
}

int main()
{
    int n;
    cin>>n;
    cout<<supSum(n);
}
